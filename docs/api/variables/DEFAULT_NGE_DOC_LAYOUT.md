---
title: DEFAULT_NGE_DOC_LAYOUT
description: The built-in default theme, dynamic-imported so it is tree-shaken away when a
---
# DEFAULT_NGE_DOC_LAYOUT

`const`

The built-in default theme, dynamic-imported so it is tree-shaken away when a
consumer ships its own theme.

```typescript
const DEFAULT_NGE_DOC_LAYOUT: NgeDocLayoutLoader
```
