---
title: withEditLink
description: Show an "Edit this page" link built from `baseUrl` and each page's compiler
---
# withEditLink

`function`

Show an "Edit this page" link built from `baseUrl` and each page's compiler
`sourcePath`, e.g. `https://github.com/org/repo/edit/main/docs`.

## Signature

```typescript
function withEditLink(baseUrl: string): NgeDocFeature
```

### Parameters

- `baseUrl` (`string`)

### Returns

`NgeDocFeature`
