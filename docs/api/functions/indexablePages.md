---
title: indexablePages
description: Every routable page across the manifests, with its breadcrumb path (site name first).
---
# indexablePages

`function`

Every routable page across the manifests, with its breadcrumb path (site name first).

## Signature

```typescript
function indexablePages(manifests: NgeDocManifest[]): object[]
```

### Parameters

- `manifests` (`NgeDocManifest[]`)

### Returns

`object[]`
