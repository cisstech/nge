---
title: joinUrl
description: Joins two url segments with exactly one slash between them.
---
# joinUrl

`function`

Joins two url segments with exactly one slash between them.

## Signature

```typescript
function joinUrl(a: string, b: string): string
```

### Parameters

- `a` (`string`)
- `b` (`string`)

### Returns

`string`
